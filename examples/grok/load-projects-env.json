{
  "description": "Ensure workspace .env is available to hook subprocesses",
  "hooks": {
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'source \"$HOME/.grok/scripts/load-projects-env.sh\" && test -n \"${TMN_COMPOSIO_API_KEY:-}${ANTHROPIC_API_KEY:-}${OPENAI_API_KEY:-}\" && echo \"projects .env loaded for hooks\" || echo \"projects .env missing or empty\"'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
